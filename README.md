# homelab

To bootstrap Flux, select your current configuration based on the path:

```
flux bootstrap github \
    --owner=mkenkel \
    --repository=homelab \
    --branch=main \
    --personal \
    --path=clusters/k8s
```
